# FileConverter

## Running Locally
1. `go run main.go`
2. open `http://localhost:80/files` in browser

## Running via Docker (WIP)
1. `docker run -it fileconverter /bin/bash`
2. `go run main.go`
3. open `http://localhost:80/files` in browser